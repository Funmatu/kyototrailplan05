{
  "name": "kyototrailplan05",
  "version": "0.1.0",
  "private": true,
  "description": "京都一周トレイル全5コース対応のガイドマップ・進捗管理アプリ (E2E only — the app itself is static HTML)",
  "scripts": {
    "test:e2e": "playwright test --config tests/playwright/playwright.config.js",
    "test:e2e:ui": "playwright test --config tests/playwright/playwright.config.js --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
