# OSM raw fetch artifacts (regenerable by tools/fetch_kyoto_trail.js)
# Whitelist ibuki CC0 GPX sources and CREDITS doc (Phase 8) — they must be
# committed because the build is reproducible only with these inputs.
data/courses/raw/*
!data/courses/raw/*.gpx
!data/courses/raw/CREDITS.md

# Editor / OS noise
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# Local server logs
/tmp/

# Node (if introduced in later phases)
node_modules/
npm-debug.log
yarn-error.log

# Playwright artifacts (Phase 6)
playwright-report/
test-results/
playwright/.cache/
